import styled from '@emotion/styled';

export const IconContainer = styled('div')({
  padding: '0 20px',
  height: '100%',
  position: 'absolute',
  pointerEvents: 'none',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
